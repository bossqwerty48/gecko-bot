# Gecko Inspector Bot

Бот для проверки наличия JPEG в HTML заголовках страниц.

## API Endpoints

- `GET /api/health` - проверка работоспособности
- `POST /api/check` - проверка списка URL

## Деплой на Render

```bash
git push origin main